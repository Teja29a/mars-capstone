# MARS — Multi-Agent Research & Summary Assistant

MARS is a multi-agent research assistant that:
- pulls live online data,
- extracts and chunks text,
- stores information in a vector memory,
- performs Retrieval-Augmented Generation (RAG),
- fact-checks content,
- and produces citation-based summaries.

## Quickstart (developer)
1. Create a Python venv and activate it:
   ```bash
   python -m venv .venv
   source .venv/bin/activate
